<body>
	<main class="show-body">
		<% if flash[:notice] %>
			<div class="flash">
				<%= flash[:notice] %>
			</div>
		<% end %>
		<p class="sbc"><span>Title :  </span><%= @book.title %></p>
		<p class="sbc"><span>Body :  </span><%= @book.body %></p>
		<div>
			<p><%= link_to "Edit", edit_book_path(@book), class: "se" %> | <%= link_to "Back", books_path, class: "sb" %></p>
		</div>
	</main>
</body>
